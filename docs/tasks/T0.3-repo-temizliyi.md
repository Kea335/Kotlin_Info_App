# T0.3 — Repo təmizliyi və lisenziya

| | |
|---|---|
| **Prioritet** | P2 |
| **Təxmin** | 0,5 gün |
| **Asılılıq** | yoxdur |
| **Branch** | `chore/repo-temizliyi` |

## Problem

1. **Commit olunmamış versiya keçidi.** Working tree-də `libs.versions.toml`
   (AGP 9.3.1 → 9.4.0) və `gradle-wrapper.properties` (Gradle 9.5.0 → 9.6.0)
   dəyişikliyi var; README cədvəli köhnə nömrələri göstərir.
2. **İstifadəsiz asılılıq.** `androidx.webkit` (`app/build.gradle.kts:87`)
   heç yerdə çağırılmır — README özü «WebView işlədilmir» yazır.
3. **Ölü funksiyalar.** `kotlinVisualTransformation` (`KotlinHighlighter.kt:241`),
   `rememberKotlinTransformation` (`:247`) — redaktor `KodRedaktoru.kt`-dəki
   keşlənmiş variantı işlədir. `ContentRepository.section()` çağırılmır.
4. **Əl ilə yazılmış versiya.** `SettingsScreen.kt:269` — `"KotlinAZ · v1.0.0"`.
   AGP 9-da `BuildConfig` defolt söndürülüb, ona görə sabit yazılıb.
5. **LICENSE yoxdur.** Portfolio repo-sunda lisenziya görünməlidir.

## Addımlar

1. Versiya keçidini commit et; README-nin «Alət / Versiya» cədvəlini
   `libs.versions.toml` ilə uyğunlaşdır: Gradle 9.6.0 · AGP 9.4.0 ·
   Kotlin 2.4.10 · Compose BOM 2026.08.00 · minSdk/targetSdk 24/37.

2. `implementation(libs.androidx.webkit)` sətrini və `libs.versions.toml`-dakı
   `webkit` girişlərini sil.

3. Ölü funksiyaları sil (3-cü bənd). `spansToPlain` (`InlineText.kt:110`)
   **saxlanılır** — T3.5-də axtarış indeksini qurmaq üçün lazım olacaq.

4. `BuildConfig`-i aç və versiyanı oradan götür:

```kotlin
// app/build.gradle.kts
buildFeatures {
    compose = true
    buildConfig = true
}
```

```kotlin
// SettingsScreen.kt
Text(text = "KotlinAZ · v${BuildConfig.VERSION_NAME}", …)
```

5. `LICENSE` (MIT) əlavə et; README-nin sonuna bir sətir:
   `Lisenziya: MIT — bax LICENSE`. Məzmunun mənbəyi qeydi olduğu kimi qalır.

## Qəbul kriteriyası

- [ ] `./gradlew assembleDebug` və `assembleRelease` keçir
- [ ] README-dəki versiyalar `libs.versions.toml` ilə eynidir
- [ ] `git status` təmizdir
- [ ] APK-da webkit kitabxanası yoxdur
- [ ] Tənzimləmələrdə versiya `versionName`-dən gəlir
- [ ] GitHub repo səhifəsində «MIT» nişanı görünür

## Diqqət

`gradle/gradle-daemon-jvm.properties` faylında `toolchainVersion=25` var —
Gradle daemon-u JDK 25 tələb edir, halbuki `compileOptions` Java 17 hədəfləyir.
Bu özlüyündə problem deyil, amma **CI-da JDK 25 qurulmalıdır** (bax T1.3).
Faylı burada silmə — qərarı T1.3-də ver.
